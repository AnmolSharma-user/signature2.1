export type AdType = "display" | "infeed" | "inarticle" | "multiplex";

interface AdContainerProps {
  type?: AdType;
  className?: string;
}

const AdContainer = (_props: AdContainerProps) => {
  return null;
};

export default AdContainer;